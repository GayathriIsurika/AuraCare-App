class OnboardingData {
  final String title;
  final String description;
  final String imagePath; // or use IconData / SVG

  const OnboardingData({
    required this.title,
    required this.description,
    required this.imagePath,
  });
}